# Claude Code Settings

Claude is used in LITL as a learning tool/socratic partner. The `settings.json` is explicitly setup in a way to prevent Claude Code from modifying the repository or its code.